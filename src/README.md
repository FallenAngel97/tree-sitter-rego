# `src`

The contents of this directory are generated from `grammar.js` by running:

```bash
npx tree-sitter generate
```

**Do not edit these files directly.** Any changes should be made to `grammar.js` in the project root, then regenerated.
These generated files are committed to the repository so that downstream users do not need npm etc to build from
scratch.
